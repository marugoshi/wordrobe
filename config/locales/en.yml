en:
  # common

  # model

  # model label

  # controller & view
  layouts:
    application:
      message:
        sign_in: "Please Sign In"
        welcome: "Welcome %{name}!"
      link:
        sign_in_with: "Sign In with %{provider}"
        log_out: "Log Out"

  static:
    welcome:
